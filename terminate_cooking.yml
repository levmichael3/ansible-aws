---

- name: Terminate instances
  hosts: localhost
  connection: local
  vars:
    region: eu-west-1
  tasks:
    - name: Terminate instances that were previously launched
      ec2:
        state: 'absent'
        region: "{{ region }}"
        instance_tags:
          step: "cooking"
